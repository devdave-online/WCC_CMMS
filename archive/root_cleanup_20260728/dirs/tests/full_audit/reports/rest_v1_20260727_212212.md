# REST API v1 full sweep

- Date: 2026-07-27T21:22:12+02:00
- Base: `http://127.0.0.1`
- Pass: **45** · Fail: **4** · Skip: **1**

```
  OK  unauth_root_401 — HTTP 401
  OK  auth_root_key — HTTP 200
  OK  auth_root_lists_resources — count=16
  OK  auth_basic_me — HTTP 200
  OK  auth_me — HTTP 200
  OK  GET /users — HTTP 200 success=true
  OK  GET /roles — HTTP 200 success=true
  OK  GET /equipment — HTTP 200 success=true
  OK  GET /production-lines — HTTP 200 success=true
  OK  GET /tickets — HTTP 200 success=true
  OK  GET /work-orders — HTTP 200 success=true
  OK  GET /inventory — HTTP 200 success=true
  OK  GET /vendors — HTTP 200 success=true
  OK  GET /purchase-orders — HTTP 200 success=true
  OK  GET /purchase-requests — HTTP 200 success=true
  OK  GET /stats — HTTP 200 success=true
  OK  GET /audit — HTTP 200 success=true
  OK  GET /ai-context — HTTP 200 success=true
  OK  GET /me — HTTP 200 success=true
  OK  GET /ticket-actions?ticket_id — HTTP 200
  OK  GET /users/1 — HTTP 200
  OK  GET /equipment/24 — HTTP 200
  OK  GET /tickets/9 — HTTP 404
  OK  GET /work-orders/39 — HTTP 200
  OK  GET /inventory/34 — HTTP 200
  OK  GET /vendors/4 — HTTP 200
  OK  GET /purchase-orders/5 — HTTP 200
  OK  GET /purchase-requests/5 — HTTP 200
  OK  GET /production-lines/1 — HTTP 200
  OK  GET /roles/1 — HTTP 200
  OK  POST /tickets — HTTP 201 id=TK-260727-001
  OK  GET created ticket — HTTP 200
  OK  PUT /tickets/{id} — HTTP 200
 FAIL POST /ticket-actions — HTTP 500 {"success":false,"message":"Unexpected error occurred.","error_code":500}
  OK  POST /equipment — HTTP 201
  OK  PUT /equipment/{id} — HTTP 200
  OK  DELETE /equipment/{id} — HTTP 200
  OK  POST /inventory — HTTP 201 body={"success":true,"data":{"part_id":"36"},"message":"Part created","timestamp":"2026-07-27T21:22:12+02
  OK  PUT /inventory/{id} — HTTP 200
  OK  DELETE /inventory/{id} — HTTP 200
 FAIL POST /vendors — HTTP 500 {"success":false,"message":"Unexpected error occurred.","error_code":500}
 FAIL POST /work-orders — HTTP 400 {"success":false,"data":null,"message":"equip_id is required","timestamp":"2026-07-27T21:22:12+02:00"}
 FAIL POST /production-lines — HTTP 400 {"success":false,"data":null,"message":"name and workshop_id are required","timestamp":"2026-07-27T2
  OK  GET /roles/4 — HTTP 200
 SKIP POST /api-keys — would rotate live admin key; already tested generate path via DB seed
  OK  GET /stats payload — HTTP 200
  OK  GET /audit — HTTP 200
  OK  GET /ai-context — HTTP 200
  OK  bad_key_401 — HTTP 401
  OK  cleanup ticket — TK-260727-001
```
