# Launch QA Report — WCC CMMS

**Date:** 2026-07-27  
**Environment:** local XAMPP (`http://127.0.0.1`, DB `workshop_db`)  
**Driver:** automated full_audit + role smoke + deep probe + DB checks  

---

## Executive result

| Gate | Result |
|------|--------|
| **Overall** | **PASS — ready for soft/internal launch** |
| Full audit (read-only) | **0 failures** |
| Full audit (`--mutate`) + deep probe | **0 failures** |
| Security gates | **20/20 OK** |
| Role HTTP smoke (tooling RBAC) | **12/12 OK** |
| i18n pack parity | **33/33 packs = 747 keys** |
| DB backup | **OK** |

---

## Actions completed this run

1. **Migrations**
   - Applied pending: `0019` tooling_documents, `0020` closed_at, `0021` locale  
   - `0009` role_definitions: table already existed without `description` — column added, migration marked applied  
   - **22/22** migration files recorded in `schema_migrations`

2. **Role presets**
   - Roles 1–4 rewritten with full `PERMISSION_LABELS` set (**24 keys**) including `view_toolings` / `manage_toolings`  
   - Custom roles 5–6 backfilled tooling keys  

3. **QoL / code**
   - Factory health machine count: `equipment WHERE deleted_at IS NULL` (fallback if column missing)  
   - Security gate updated for i18n login status check  

4. **Backup**
   - `C:\xampp\htdocs\backups\pre_launch_20260727_220101\workshop_db_20260727_220101.sql` (~428 KB)  
   - Snapshots: `role_definitions.json`, `users_snapshot.json`

---

## Test evidence

### Full audit
- Report: `tests/full_audit/reports/audit_20260727_210131.md` (clean re-run)  
- Mutate + deep probe: `audit_20260727_210157.md`  
- Deep probe standalone: **14 ok, 0 fail** (tooling symbology, BOM, docs upload, cleanup)

### Security gates
All 20 checks green (CSRF, CLI guards, login status, password handling, etc.)

### RBAC matrix (DB)

| Role | view_eq | manage_eq | view_toolings | manage_toolings |
|------|---------|-----------|---------------|-----------------|
| L1 Operator | Y | n | Y | n |
| L2 Technician | Y | n | Y | n |
| L3 Supervisor | Y | Y | Y | Y |
| L4 Admin | Y | Y | Y | Y |
| L5 Custom Viewer | n | n | n | n |
| L6 Storekeeper | Y | n | Y | n |

### HTTP role smoke

| Case | Expected | Result |
|------|----------|--------|
| Admin tooling ledger | allow | OK |
| Admin tooling vault | allow | OK |
| Operator tooling ledger | allow | OK |
| Operator tooling vault | **deny** | OK |
| Tech tooling ledger | allow | OK |
| Tech tooling vault | **deny** | OK |
| Viewer tooling / equipment | **deny** | OK |
| Supervisor tooling vault | allow | OK |
| Admin profile (language picker) | allow + picker present | OK |
| Active tickets / history | allow | OK |

### Other checks

| Check | Result |
|-------|--------|
| users.locale column | present |
| closed_at column | present |
| toolings / bom / documents tables | present |
| Equipment active vs total | 24 / 24 (0 soft-deleted) |
| Demo users must_change_password | all 0 (already changed) |
| i18n packs missing keys | 0 |

---

## Known non-blockers (post-launch OK)

- REST v1 with API key: skipped (no `api_key` issued in DB)  
- Notifications API GET returns 403 under current perm model (expected by suite)  
- Mutate ticket create optional path SKIPs unless `--mutate` (mutate suite + deep probe covered tooling mutates)  
- Deep admin modal field-level i18n incomplete  
- Notification **message bodies** still stored in English  
- RTL CSS polish for ar/ur not fully QA’d visually  
- `create_tooling` form POST may return HTTP 403/302 quirk in probe but tooling was created and cleaned up (suite treats as OK)

---

## Launch recommendation

**GO for soft/internal launch** on this host after:

1. Hard-refresh browsers (`WCC_UI_VERSION` **2.7.0**)  
2. Keep backup folder listed above  
3. Optionally issue REST API keys only when integrations need them  

**Not required before soft launch:** further i18n depth, RTL CSS, live notification translation.
